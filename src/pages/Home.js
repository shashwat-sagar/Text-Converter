import React from "react";
import TextForm from "../components/TextForm";

const Home = (props) => {
 
  
  return (
   <div>
     <TextForm heading={'TEXT Converter'} />
   </div>
  );
};

export default Home;

